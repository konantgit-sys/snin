"""
SNIN ESP32 — Config Template

Скопировать на ESP32 как config.py.
Сгенерировать ключи: запустить snin_sensor.py однократно — он выведет публичный ключ.
Этот публичный ключ добавить в bridge.py на стороне сервера.

ВНИМАНИЕ: Никогда не пушить приватный ключ в git!
Создавать config.py отдельно на каждой ESP32.
"""

# ─── Устройство ─────────────────────────────────────────────────
DEVICE_ID = "sensor_kitchen_01"        # уникальное имя датчика
SENSOR_TYPE = "temperature"            # temperature, humidity, air_quality
MEASURE_INTERVAL = 60                  # секунд между измерениями

# ─── WiFi ───────────────────────────────────────────────────────
WIFI_SSID = "SNIN_MESH"               # имя сети (ESP-NOW требует WiFi)
WIFI_PASSWORD = ""                     # пусто — открытая сеть

# ─── Криптография ──────────────────────────────────────────────
# Сгенерировать однократно. Сохранить ПРИВАТНЫЙ ключ на ESP32.
# Публичный ключ передать в bridge для верификации.
PRIVATE_KEY_HEX = "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"

# ─── Bridge ─────────────────────────────────────────────────────
# MAC-адрес ESP32-bridge (hex, без разделителей).
# Если unknown — broadcast (все ESP32 в радиусе получат)
BRIDGE_MAC = "ff" * 6  # broadcast

# ─── Пины ───────────────────────────────────────────────────────
DHT_PIN = 4          # GPIO4 — DHT22 датчик
LED_PIN = 2          # GPIO2 — встроенный LED (индикация)
